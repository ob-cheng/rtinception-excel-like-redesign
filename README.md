
  # RTInception Excel-like Redesign (Tianen)

  This is a code bundle for RTInception Excel-like Redesign (Tianen). 
  
   ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  